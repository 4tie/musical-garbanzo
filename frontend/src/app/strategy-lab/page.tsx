import PlaceholderPage from '@/components/PlaceholderPage';

export default function StrategyLab() {
  return (
    <PlaceholderPage
      title="Strategies"
      description="Legacy route placeholder. Strategy creation workflows are not part of this read-only dashboard foundation."
      focus="Strategy route compatibility"
    />
  );
}
