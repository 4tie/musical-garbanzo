import PlaceholderPage from '@/components/PlaceholderPage';

export default function Results() {
  return (
    <PlaceholderPage
      title="Reports"
      description="Legacy route placeholder. Reports and artifacts remain read-only and use real API data in later prompts."
      focus="Reports route compatibility"
    />
  );
}
