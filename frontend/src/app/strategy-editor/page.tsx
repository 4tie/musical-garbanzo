import PlaceholderPage from '@/components/PlaceholderPage';

export default function StrategyEditor() {
  return (
    <PlaceholderPage
      title="Strategy Editor"
      description="Legacy route placeholder. Editing, approval, and export actions are not part of this read-only foundation."
      focus="Editor route compatibility"
    />
  );
}
