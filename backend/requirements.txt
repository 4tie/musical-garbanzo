# HER Backend Requirements
# Local-only trading strategy validation system

# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23
aiosqlite==0.19.0

# HTTP Client
httpx==0.25.2

# Freqtrade real execution
# IMPORTANT: Use scripts/install-backend.sh for a deterministic install.
# freqtrade's transitive deps can pull in a newer FastAPI; the script pins
# the correct versions in a second pass. See knowledge_base/RUNBOOK.md.
freqtrade[hyperopt]==2026.6

# Environment Variables
python-dotenv==1.0.0

# Logging
python-json-logger==2.0.7

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Code Quality (optional)
ruff==0.1.8
